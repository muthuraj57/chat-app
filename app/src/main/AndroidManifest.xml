<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.muthuraj.gossip" >
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.muthuraj.gossip.SignInActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectChat"/>
        <activity android:name=".PublicChat"/>
        <activity android:name=".AllUsers"/>
        <activity android:name=".OnlineUsers"/>
        <activity android:name=".PrivateChat"/>
        <activity android:name=".ChatActivity"/>
        <activity android:name=".About"/>

    </application>

</manifest>
